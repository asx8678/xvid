{
  "name": "tweet-video-saver",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension to save videos from X.com and Twitter posts",
  "scripts": {
    "build": "mkdir -p dist && cd dist && rm -f tweet-video-saver.zip && cd .. && zip -r dist/tweet-video-saver.zip manifest.json background/ content/ popup/ options/ lib/ shared/ icons/ _locales/ -x '*.DS_Store'",
    "test": "vitest run"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
